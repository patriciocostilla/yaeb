apiVersion: v1
kind: Service
metadata:
  name: {{ include "yaeb.fullname" . }}
  labels:
    {{- include "yaeb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "yaeb.selectorLabels" . | nindent 4 }}
