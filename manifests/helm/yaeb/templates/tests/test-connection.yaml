apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yaeb.fullname" . }}-test-connection"
  labels:
    {{- include "yaeb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yaeb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
